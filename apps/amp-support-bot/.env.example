# Ollama base URL (embedding + chat). Default: http://127.0.0.1:11434
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Embedding model available in Ollama (e.g., mxbai-embed-large or nomic-embed-text)
EMBED_MODEL=mxbai-embed-large
# Chat model (e.g., llama3:8b)
CHAT_MODEL=llama3:8b
# Root data dir (relative to repo root by default)
DATA_ROOT=./root
# Chroma DB directory (relative or absolute). Default is ${DATA_ROOT}/chroma
CHROMA_DIR=./root/chroma
